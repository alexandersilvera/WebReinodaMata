---
/**
 * Página de confirmación de pago exitoso
 */
import MainLayout from '@/layouts/MainLayout.astro';
export const prerender = false;
---

<MainLayout title="Pago Exitoso | Centro Umbandista Reino Da Mata">
  <div class="min-h-screen bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900 py-16 flex items-center justify-center">
    <div class="max-w-2xl mx-auto px-4">
      <div class="bg-slate-800/50 backdrop-blur-sm border border-slate-700/50 rounded-xl p-8 text-center">
        <!-- Success Icon -->
        <div class="mb-6">
          <div class="w-20 h-20 bg-green-500/20 rounded-full flex items-center justify-center mx-auto">
            <svg class="w-12 h-12 text-green-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
            </svg>
          </div>
        </div>

        <!-- Title -->
        <h1 class="text-3xl font-bold text-white mb-4">
          ¡Pago Exitoso!
        </h1>

        <!-- Message -->
        <p class="text-gray-300 mb-8">
          Tu suscripción ha sido activada correctamente. Ahora tienes acceso completo a todos los beneficios de tu plan.
        </p>

        <!-- Payment Details -->
        <div class="bg-slate-900/50 rounded-lg p-6 mb-8 text-left">
          <h3 class="text-lg font-bold text-white mb-4">Detalles del pago</h3>
          <div class="space-y-2 text-gray-300">
            <div class="flex justify-between">
              <span>ID de Transacción:</span>
              <span id="payment-id" class="font-mono text-sm">-</span>
            </div>
            <div class="flex justify-between">
              <span>Estado:</span>
              <span class="text-green-400 font-medium">Aprobado</span>
            </div>
          </div>
        </div>

        <!-- Next Steps -->
        <div class="bg-blue-900/30 border border-blue-500/50 rounded-lg p-6 mb-8 text-left">
          <h3 class="text-lg font-bold text-white mb-3">Próximos pasos:</h3>
          <ul class="space-y-2 text-gray-300">
            <li class="flex items-start">
              <svg class="w-5 h-5 text-blue-400 mr-2 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
              </svg>
              Revisa tu correo electrónico para la confirmación
            </li>
            <li class="flex items-start">
              <svg class="w-5 h-5 text-blue-400 mr-2 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
              </svg>
              Accede al contenido premium desde tu perfil
            </li>
            <li class="flex items-start">
              <svg class="w-5 h-5 text-blue-400 mr-2 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
              </svg>
              Explora los eventos y recursos disponibles
            </li>
          </ul>
        </div>

        <!-- Actions -->
        <div class="flex flex-col sm:flex-row gap-4">
          <a
            href="/perfil"
            class="flex-1 py-3 px-6 bg-blue-600 hover:bg-blue-500 text-white font-medium rounded-lg text-center transition-colors"
          >
            Ir a Mi Perfil
          </a>
          <a
            href="/blog"
            class="flex-1 py-3 px-6 bg-slate-700 hover:bg-slate-600 text-white font-medium rounded-lg text-center transition-colors"
          >
            Explorar Contenido
          </a>
        </div>
      </div>
    </div>
  </div>
</MainLayout>

<script>
  // Get payment ID from URL
  const urlParams = new URLSearchParams(window.location.search);
  const paymentId = urlParams.get('payment_id');
  const externalRef = urlParams.get('external_reference');

  if (paymentId) {
    document.getElementById('payment-id').textContent = paymentId;
  }

  // Log for analytics
  console.log('Payment successful:', { paymentId, externalRef });
</script>
