---
/**
 * Página de pago pendiente
 */
import MainLayout from '@/layouts/MainLayout.astro';
export const prerender = false;
---

<MainLayout title="Pago Pendiente | Centro Umbandista Reino Da Mata">
  <div class="min-h-screen bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900 py-16 flex items-center justify-center">
    <div class="max-w-2xl mx-auto px-4">
      <div class="bg-slate-800/50 backdrop-blur-sm border border-slate-700/50 rounded-xl p-8 text-center">
        <!-- Pending Icon -->
        <div class="mb-6">
          <div class="w-20 h-20 bg-yellow-500/20 rounded-full flex items-center justify-center mx-auto">
            <svg class="w-12 h-12 text-yellow-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
          </div>
        </div>

        <!-- Title -->
        <h1 class="text-3xl font-bold text-white mb-4">
          Pago Pendiente
        </h1>

        <!-- Message -->
        <p class="text-gray-300 mb-8">
          Tu pago está siendo procesado. Te notificaremos por correo electrónico cuando se confirme.
        </p>

        <!-- Payment Details -->
        <div class="bg-slate-900/50 rounded-lg p-6 mb-8 text-left">
          <h3 class="text-lg font-bold text-white mb-4">Detalles del pago</h3>
          <div class="space-y-2 text-gray-300">
            <div class="flex justify-between">
              <span>ID de Transacción:</span>
              <span id="payment-id" class="font-mono text-sm">-</span>
            </div>
            <div class="flex justify-between">
              <span>Estado:</span>
              <span class="text-yellow-400 font-medium">Pendiente</span>
            </div>
          </div>
        </div>

        <!-- Info -->
        <div class="bg-yellow-900/30 border border-yellow-500/50 rounded-lg p-6 mb-8 text-left">
          <h3 class="text-lg font-bold text-white mb-3">¿Qué significa esto?</h3>
          <ul class="space-y-2 text-gray-300">
            <li class="flex items-start">
              <svg class="w-5 h-5 text-yellow-400 mr-2 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"></path>
              </svg>
              Tu pago está en proceso de verificación
            </li>
            <li class="flex items-start">
              <svg class="w-5 h-5 text-yellow-400 mr-2 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"></path>
              </svg>
              Si pagaste en efectivo, puede tardar hasta 48 horas
            </li>
            <li class="flex items-start">
              <svg class="w-5 h-5 text-yellow-400 mr-2 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"></path>
              </svg>
              Recibirás un correo cuando se apruebe el pago
            </li>
          </ul>
        </div>

        <!-- Actions -->
        <div class="flex flex-col sm:flex-row gap-4">
          <a
            href="/"
            class="flex-1 py-3 px-6 bg-blue-600 hover:bg-blue-500 text-white font-medium rounded-lg text-center transition-colors"
          >
            Volver al Inicio
          </a>
          <a
            href="/contacto"
            class="flex-1 py-3 px-6 bg-slate-700 hover:bg-slate-600 text-white font-medium rounded-lg text-center transition-colors"
          >
            Contactar Soporte
          </a>
        </div>
      </div>
    </div>
  </div>
</MainLayout>

<script>
  // Get payment ID from URL
  const urlParams = new URLSearchParams(window.location.search);
  const paymentId = urlParams.get('payment_id');
  const externalRef = urlParams.get('external_reference');

  if (paymentId) {
    document.getElementById('payment-id').textContent = paymentId;
  }

  // Log for analytics
  console.log('Payment pending:', { paymentId, externalRef });
</script>
